export default interface Plains {
  id: string;
  prize: string;
  comments: string;
  title: string;
  sellerId: string;
  done: boolean;
  supervisorId: string;
  visitId: string;
  moduleId: string;
}
